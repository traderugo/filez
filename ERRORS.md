there should be no need to invite a user and create an account for them, instead, let the invite just show up in the invited user's dashboard. they will see it when they signup. so... there is no need to reset or create passwords for staffs. so the modal should just show a "join station" text with link to signup... it's a bad idea for a user to manage other user's auth

the subscriptions should be tied to a station, for now, it's global for all stations, which is wrong. so subscription links or logic shouldn't be in the dashboard. instead it should show on the station page.

