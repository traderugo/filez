I'M THINKING OF A DIFFERENT APPROACH
1. instead of sharing links to onedrive files, how about i save empty "template excel files" in supabase storage
2. you build a ui that mirrors each of the files and also doing some tweaks based on the data provided during onboarding
3. users can then crud the files + edit logs
4. users can then download the excel sheet along with the manipulated data if they need it (just values, no formula)
5. the pages can be private (i.e the subscribing user can save a list of emails that can access them) or public (everyone can access) if they have the join link

THE LIST OF EXCEL FILE TEMPLATES
1. DAILY SALES OPERATION 
2. LUBE LOGS

the excel files are in the station-portal/excel folder